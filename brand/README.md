# Brand assets

Place final production assets here:

- `logo.png` — 1024×1024 app icon source
- `logo-dark.png` — dark background variant
- `favicon.png` — web favicon

Current app icons live in `assets/images/` and `assets/expo.icon/`.
