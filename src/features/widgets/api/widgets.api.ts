import type { WidgetBirthdayPreview } from '../types';

// Native iOS/Android widgets — implement with expo-widgets or custom native module
export async function getUpcomingBirthdaysForWidget(): Promise<WidgetBirthdayPreview[]> {
  return [];
}
